[project]
name = "ppt-builder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "markdown>=3.5",
    "pillow>=10.0",
    "playwright>=1.40",
    "pydantic>=2.5.0",
    "pygments>=2.16",
    "python-pptx>=1.0.0",
    "pyyaml>=6.0",
]
